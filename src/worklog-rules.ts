export const WORKLOG_RULES_MARKDOWN = `# 工作日志记录规则

## 1. 工作项编号规则
- 每日工作项在展示、复制、导出时，都应以当日序号开头，例如：\`1. 修复筛选回显\`。
- 录入时不要手工保留序号；系统会按当天顺序自动补号。
- 若输入内容已带类似 \`1. \`、\`2. \` 的前缀，写入前应去掉，再由系统统一编号。

## 2. 工作项润色规则
- 只保留客观事实，不虚构结果，不夸大价值。
- 一条工作项只描述一件主要事项，尽量短句化。
- 优先使用可复盘的动作词，如“排查 / 修复 / 联调 / 补齐 / 重构 / 验证 / 发布 / 回滚 / 跟进”。
- 避免空话套话，如“处理了一些事情”“做了很多优化”“推进相关工作”。
- 不写周报口吻，不写主观抒情，不写和工时无关的修辞。

## 3. 锐评添加规则
- 锐评与工作项分开维护，不混写到工作项里。
- 锐评优先记录：阻塞、取舍、风险、返工原因、协作问题、后续建议、经验总结。
- 若当天只是常规执行、信息量不足，可以不给锐评。
- 锐评要求简洁、具体、可复盘，避免口号化、空泛总结。
- 默认不建议给“今天”即时补锐评，除非配置明确允许。

## 4. AI 辅助规则
- AI 只能基于已有工作项做润色或建议，不得捏造未发生事项。
- AI 润色后的工作项仍需符合“系统自动编号”的规则，即返回内容本身不带手工序号。
- AI 检测锐评时，应先判断“值不值得写”，再决定是否给建议文案。
- 若 AI 判断不值得写，应明确返回“不建议补锐评”及原因。`;
